1，先导入，导入以后，再有一个按钮(导入)，当导入完成以后，点击导入，现在才开始导入，因为导入数据很慢，所以判断里面的条数
2，现在80万条导入花80分钟，按导入的条数来判断时间，一点单击了导入，提示需要的大概时间，然后生成一个key，有一个按钮是检
测是否导入完成，导入完成以后才可再次导入或者做其他事。
3，删除以后，做tran table 回收高水位。
4，到对比界面后，只列出俩表的前10条，然后选择要对比的列。

这些都是后续要做的。


在导入的时候，有个前提，必须选择要导入多少列，默认15列

---------------------------------
现在实现了的，上传功能已经实现。
导入功能已经实现。

接下来要做的就是细化功能。   这里的每一步都设置一个key，用以标记是否已经做了这一步
1，点了上传，置灰，
2，点击导入前有个设置按钮，从第几行开始是数据。默认第二行，还有列数限制，默认11列  ，就现在这个项目来说。
ShiroUser acount = (ShiroUser)SecurityUtils.getSubject().getPrincipal();  获取当前登录的用户信息


